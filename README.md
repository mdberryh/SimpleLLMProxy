# SimpleLLMProxy
LLM Proxy for local models. This was created to do several tasks 1) loadbalance a dual GPU setup or more. 2) allow llmlingua to reduce context if desired. 3) shutdown AMD docker containers when not in use to save power.
